<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.personal.korail_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".scheduleStaionSearch" />
        <activity android:name=".scheduleStationInfo" />
        <activity android:name=".lostAndFoundSearch" />
        <activity android:name=".lostAndFoundCallNLocation" />
        <activity android:name=".lostAndFoundStep" />
        <activity android:name=".EventDetailActivity" />
        <activity android:name=".EventListActivity" />
        <activity android:name=".CulturalFacilitiesDetailActivity" />
        <activity android:name=".CulturalFacilitiesListActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".ChattingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".service.HTTPConnectionService" />
    </application>

</manifest>